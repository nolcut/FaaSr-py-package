from setuptools import setup, find_packages

setup(
    name='FaaSr_py',
    version='0.1.0',
    packages=find_packages(),
    # You can also add other arguments such as author, description, install_requires etc.
)